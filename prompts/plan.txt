请先不要修改任何文件。

Outcome：
- 将 inputs 目录中的虚构市场资料转化为一个面向企业高管的单页交互式决策网页。
- 网页应回答优先进入哪个市场、为什么，以及战略偏好变化后结论是否改变。

Constraints：
- 阅读项目 CLAUDE.md 和 inputs 下全部文件。
- 使用纯 HTML、CSS、JavaScript，不依赖外部 CDN、框架、后端或构建工具。
- 最终只在根目录新建或修改 site/ 下的文件，不修改 inputs/ 和 reference/。
- 页面必须适配桌面和手机。
- 关键数字要显示来源；明确区分事实、推断和假设。
- 推荐结论必须展示最强反对理由和数据缺口。
- 排名必须使用 data-dictionary.md 定义的方向与归一化逻辑。

Before proposing the plan：
- 检查输入之间的冲突和缺失信息；
- 识别需要的页面区域、交互、数据结构和计算逻辑；
- 说明哪些决策真正需要我确认；
- 比较至少两种实现方式，并推荐课堂可靠性更高的一种。

Plan must include：
- 页面信息架构与用户路径；
- 评分和情景权重模型；
- 具体文件和实施阶段；
- 无障碍、响应式和错误处理；
- 浏览器验收、代码检查及成功标准；
- 风险、非目标和回退办法。

提交计划后等待我的审批，不要开始实现。
